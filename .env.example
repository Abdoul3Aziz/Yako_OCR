# Copier vers .env sur le serveur si besoin (ne pas committer .env)

# rapid | paddle | hybrid
OCR_BACKEND=hybrid

# 1 = précharge OCR au démarrage (peut timeout sur cPanel)
# 0 = charge au premier appel OCR
OCR_WARMUP=0

OCR_FALLBACK_MIN_MISSING=2
PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK=True
